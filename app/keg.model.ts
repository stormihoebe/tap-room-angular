export class Keg{
  constructor (public name: string, public brand: string, public type: string, public description: string, public price: number, public abv: number, public quantity: number, public image: string){}
}
